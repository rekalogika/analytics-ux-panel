{%- block filter -%}
    <input type="text"
           readonly
           disabled
           class="form-control text-secondary"
           placeholder="{{ t('This dimension does not support filtering.', domain = 'rekalogika_analytics', domain = 'rekalogika_analytics')|trans }}">
{%- endblock filter -%}
